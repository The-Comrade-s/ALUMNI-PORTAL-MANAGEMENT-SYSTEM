# The backend API's public URL — no trailing slash.
# Local:      http://localhost:8000
# Production: your Railway service URL, e.g. https://gateway-alumni-api.up.railway.app
# Read by lib/api-client.ts for every API call, and by app/(admin)/admin/reports
# for direct-download report links.
NEXT_PUBLIC_API_URL=http://localhost:8000
